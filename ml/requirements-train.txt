# Optional training dependencies for Snapback offline ML.
# Install from repo root:
#   pip install -r ml/requirements-train.txt
#
# macOS: brew install libomp  (needed for xgboost)

xgboost>=3.3.0
onnx>=1.16.0
onnxmltools>=1.12.0
onnxruntime>=1.16.0
